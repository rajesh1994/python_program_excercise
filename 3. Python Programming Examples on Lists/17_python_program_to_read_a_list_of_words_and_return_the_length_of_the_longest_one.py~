"""
Problem Statement : Python program to read a list of words and return the length of the longest one
"""

l1 = []
n = int(input("Enter the number of elements in the list:"))

for i in range(0, n):
    l2 = input("Enter an element" + str(i+1) + ":")
    l1.append(l2)

max1 = len(l1[0])
temp = l1[0]

for j in l1:
    if len(j) > max1:
        max1 = len(j)
        temp = j
print ("The word with the longest length is:")
print (temp)
